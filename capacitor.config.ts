import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.divcom.pokedex_vuetify',
  appName: 'Pokédex Vuetify',
  webDir: 'dist'
};

export default config;
